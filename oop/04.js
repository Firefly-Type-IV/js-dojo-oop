/**
 * Creates a rectangle object with width, height, and area calculation.
 *
 * @param {number} width - The rectangle's width.
 * @param {number} height - The rectangle's height.
 * @returns {Object} - A rectangle object with getArea method.
 *
 * @example
 * // returns 20
 * createRectangle(4, 5).getArea();
 */
export function createRectangle(width, height) {
  // TODO: Create an object with width and height properties.
  // TODO: Add a getArea method that returns width * height.
}
