/**
 * A Car class with make, model, and a method to get full name.
 */
export class Car {
  /**
   * @param {string} make - The car's make (e.g., "Toyota").
   * @param {string} model - The car's model (e.g., "Camry").
   */
  constructor(make, model) {
    // TODO: Store make and model as properties.
  }

  /**
   * Gets the full name of the car.
   * @returns {string} - The make and model combined.
   *
   * @example
   * // returns "Toyota Camry"
   * new Car("Toyota", "Camry").getFullName();
   */
  getFullName() {
    // TODO: Return a string with make and model separated by a space.
  }
}
